import React from 'react';
import Button from '../../../../UI/Button/Button';

const drawerToggle = (props) => (
    <div>
        <Button clicked={props.clicked}>MENU</Button>
    </div>
);


export default drawerToggle;